- if @user.errors.any?
  #error_explanation
    .alert.alert-danger
      = "The form contains the #{pluralize(@user.errors.count, "error")}"
      %ul
      - @user.errors.full_messages.each do |msg|
        %li= msg